import { AnalysisPageFrame } from '@astra-spec/theme-astra/shell';

export function ArticlePageAndNavigation({ children }: { children: React.ReactNode }) {
  return <AnalysisPageFrame>{children}</AnalysisPageFrame>;
}
